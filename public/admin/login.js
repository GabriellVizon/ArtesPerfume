document.addEventListener('DOMContentLoaded',async()=>{try{await AdminAP.api('/api/auth/me');location.replace('/admin/dashboard.html');return}catch{}const form=document.getElementById('login-form'),error=document.getElementById('login-error'),button=document.getElementById('login-button');form.addEventListener('submit',async e=>{e.preventDefault();error.hidden=true;button.disabled=true;button.textContent='Autenticando…';try{await AdminAP.api('/api/auth/login',{method:'POST',body:JSON.stringify({email:document.getElementById('email').value.trim(),senha:document.getElementById('password').value})});location.replace('/admin/dashboard.html')}catch(err){error.textContent=err.message;error.hidden=false;button.disabled=false;button.textContent='Entrar no painel →'}})});
